{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Control Type",
  "$id": "cmolitor.test-annotations.controlType",
  "type": "string",
  "description": "Control samples suitable for normalization and batch correction",
  "enum": [
    "GIS"
  ]
}
